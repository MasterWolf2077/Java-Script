// Logical Operators

//Scholarship Eligibility
let grade = 85;
let attendance = 90;

console.log((grade >= 80 && attendance >= 85) ? "Scholarship Approved" : "Not Approved");

// Voting Eligibility
let age = 20;
let citizenship = "SA";

console.log((age >= 18 && citizenship === "SA") ? "Eligible to vote" : "Not Eligible");